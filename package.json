{
  "name": "shaul1991-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:tokens && node scripts/build-css.mjs",
    "build:tokens": "design.md export DESIGN.md --format dtcg > tokens.json",
    "lint:design": "design.md lint DESIGN.md",
    "preview": "python3 -m http.server 4173",
    "verify": "npm run lint:design && npm run build && node scripts/verify-build.mjs"
  },
  "devDependencies": {
    "@google/design.md": "0.4.0",
    "lightningcss": "1.33.0"
  }
}
